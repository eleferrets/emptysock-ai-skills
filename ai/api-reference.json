{
  "meta": {
    "engine": "EmptySock",
    "version": "1.0.0",
    "package": "@emptysock/engine",
    "description": "Machine-readable public API reference. Load at agent task start for full typed context. No internal implementation details.",
    "repo": "https://github.com/your-org/emptysock-agent-pack"
  },
  "stack": {
    "renderer2d": "PixiJS v8 (WebGL2 primary, WebGPU where available)",
    "renderer3d": "Three.js (optional, hybrid scenes only)",
    "physics": "Rapier2D WASM (@dimforge/rapier2d)",
    "audio": "Howler.js",
    "shell": "Tauri v2",
    "language": "TypeScript strict mode"
  },
  "rules": {
    "banned_imports": [
      "pixi.js",
      "@dimforge/rapier2d",
      "howler",
      "three"
    ],
    "banned_patterns": [
      "any type",
      "non-null assertion (!)",
      "ts-ignore",
      "setTimeout in game logic",
      "setInterval in game logic",
      "async onUpdate()",
      "await inside onUpdate()",
      "document.querySelector",
      "document.getElementById",
      "JSON.parse(x) as T — always use Zod",
      "window.localStorage — use SaveSystem"
    ],
    "required_patterns": [
      "explicit return types on all functions",
      "import type for type-only imports",
      "Zod schema for all external data",
      "entity.destroy() when entity is done",
      "TimerHandle.cancel() in onDestroy()",
      "conventional commits: feat/fix/refactor/chore(scope): message"
    ]
  },
  "systems": {
    "Engine": {
      "import": "import { Engine } from '@emptysock/engine'",
      "methods": {
        "create": {
          "signature": "Engine.create(config: EngineConfig): Promise<Engine>",
          "config": {
            "scenes": "Record<string, typeof Scene> — all scene classes",
            "startScene": "string — name of first scene to load",
            "gameSpeed": "number — target FPS, default 60",
            "rendererType": "'auto' | 'webgpu' | 'webgl2' — default 'auto'",
            "splash": "boolean — show splash on boot, default false"
          }
        },
        "start": "engine.start(): Promise<void>",
        "stop": "engine.stop(): void",
        "pushScene": "Engine.pushScene(scene: Scene): void — push a scene on the stack (mirrors SceneManager.pushScene; use this.engine.pushScene() from inside a Scene)",
        "popScene": "Engine.popScene(): void — pop the top scene (use this.engine.popScene() from inside a Scene)",
        "loadScene": "Engine.loadScene(name: string): void — load a named scene, replacing the active scene (use this.engine.loadScene() from inside a Scene)"
      },
      "properties": {
        "gpuTier": "'potato' | 'low' | 'mid' | 'high' | 'ultra' — read-only"
      }
    },
    "Scene": {
      "import": "import { Scene, type SceneConfig } from '@emptysock/engine'",
      "description": "Base class. Extend for every game scene.",
      "static": {
        "config": "SceneConfig — declare as static readonly on each subclass"
      },
      "SceneConfig": {
        "renderMode": "'2d' | '3d' | 'hybrid'",
        "gameSpeed": "number — overrides engine default for this scene",
        "physics": "boolean — enable Rapier world, default true",
        "lighting": "boolean — enable 2D lighting pass, default false",
        "backgroundColor": "number — hex colour"
      },
      "lifecycle": {
        "onLoad": "async onLoad(): Promise<void> — awaited before scene renders",
        "onStart": "onStart(): void — called once after onLoad",
        "onUpdate": "onUpdate(dt: number): void — every frame, dt = seconds",
        "onFixedUpdate": "onFixedUpdate(dt: number): void — fixed 1/60s timestep",
        "onDestroy": "onDestroy(): void — before scene unloads",
        "onPause": "onPause(): void — when scene is pushed under another",
        "onResume": "onResume(): void — when scene returns to top"
      },
      "methods": {
        "createEntity": "scene.createEntity(name: string, opts?: { position?: Point, rotation?: number, scale?: Point }): Entity",
        "getEntity": "scene.getEntity(name: string): Entity | undefined",
        "getEntitiesByTag": "scene.getEntitiesByTag(tag: string): Entity[]",
        "getEntitiesWithComponent": "scene.getEntitiesWithComponent(type: ComponentType): Entity[]",
        "instantiatePrefab": "scene.instantiatePrefab(file: string, opts?: PrefabOptions): Entity",
        "createParticleEmitter": "scene.createParticleEmitter(opts: ParticleEmitterOptions): ParticleEmitter"
      },
      "properties": {
        "lighting.ambientColour": "number (hex)",
        "lighting.ambientIntensity": "number (0–1)",
        "postProcess": {
          "add": "scene.postProcess.add(effect: PostEffect, opts?: object): void",
          "remove": "scene.postProcess.remove(effect: PostEffect): void",
          "flash": "scene.postProcess.flash(opts: FlashOptions): void"
        }
      }
    },
    "SceneManager": {
      "import": "import { SceneManager } from '@emptysock/engine'",
      "methods": {
        "load": "SceneManager.load(name: string, opts?: LoadOptions): Promise<void>",
        "transition": "SceneManager.transition(name: string, opts: TransitionOptions): Promise<void>",
        "push": "SceneManager.push(name: string): Promise<void>",
        "pop": "SceneManager.pop(): Promise<void>",
        "preload": "SceneManager.preload(name: string): Promise<void>",
        "pushScene": "SceneManager.pushScene(scene: Scene): void — push a scene instance on top of the stack; active scene is paused",
        "popScene": "SceneManager.popScene(): void — pop top scene; scene underneath resumes"
      },
      "properties": {
        "currentScene": "string — name of active scene",
        "stackDepth": "number — current scene stack depth"
      },
      "TransitionOptions": {
        "effect": "'fade' | 'wipe' | 'iris' | 'slide' | 'zoom' | 'dissolve' | 'flash' | 'custom'",
        "duration": "number (seconds)",
        "colour": "number (hex, for fade/flash)",
        "direction": "'left' | 'right' | 'up' | 'down' (for wipe/slide)",
        "origin": "Point (for iris/zoom)"
      }
    },
    "Entity": {
      "import": "import { Entity } from '@emptysock/engine'",
      "properties": {
        "name": "string",
        "position": "{ x: number, y: number }",
        "globalPosition": "{ x: number, y: number } — world-space",
        "rotation": "number — radians",
        "rotationDegrees": "number",
        "scale": "{ x: number, y: number }",
        "active": "boolean",
        "isAlive": "boolean"
      },
      "methods": {
        "addComponent": "entity.addComponent<T>(type: new() => T, props?: Partial<T>): T",
        "getComponent": "entity.getComponent<T>(type: new() => T): T | undefined",
        "requireComponent": "entity.requireComponent<T>(type: new() => T): T",
        "hasComponent": "entity.hasComponent(type: ComponentType): boolean",
        "removeComponent": "entity.removeComponent(type: ComponentType): void",
        "addTag": "entity.addTag(tag: string): void",
        "removeTag": "entity.removeTag(tag: string): void",
        "hasTag": "entity.hasTag(tag: string): boolean",
        "setParent": "entity.setParent(parent: Entity | null): void",
        "setPosition": "entity.setPosition(x: number, y: number): void",
        "translate": "entity.translate(dx: number, dy: number): void",
        "rotateTo": "entity.rotateTo(radians: number, opts?: TweenOptions): void",
        "distanceTo": "entity.distanceTo(other: Entity): number",
        "angleTo": "entity.angleTo(other: Entity): number",
        "destroy": "entity.destroy(): void",
        "emit": "entity.emit(event: string, data?: unknown): void",
        "on": "entity.on(event: string, fn: (data: unknown) => void): void",
        "once": "entity.once(event: string, fn: (data: unknown) => void): void",
        "off": "entity.off(event: string, fn: Function): void",
        "onCollisionEnter": "entity.onCollisionEnter(fn: (other: Entity, contact: ContactInfo) => void): void",
        "onCollisionExit": "entity.onCollisionExit(fn: (other: Entity) => void): void",
        "onSensorEnter": "entity.onSensorEnter(fn: (other: Entity) => void): void",
        "onSensorExit": "entity.onSensorExit(fn: (other: Entity) => void): void",
        "startCoroutine": "entity.startCoroutine(gen: GeneratorFunction, name?: string): void",
        "stopCoroutine": "entity.stopCoroutine(name: string): void"
      }
    },
    "components": {
      "Sprite": {
        "import": "import { Sprite } from '@emptysock/engine'",
        "props": {
          "texture": "string — filename in assets/sprites/",
          "frame": "string — atlas frame name (optional)",
          "anchor": "{ x: number, y: number } — 0–1, default { x: 0.5, y: 0.5 }",
          "tint": "number — hex, default 0xffffff",
          "alpha": "number — 0–1",
          "blendMode": "'normal' | 'add' | 'multiply' | 'screen'",
          "flipX": "boolean",
          "flipY": "boolean",
          "normalMap": "string — filename for 2D lighting normal map (optional)"
        }
      },
      "Animator": {
        "import": "import { Animator } from '@emptysock/engine'",
        "props": {
          "spritesheet": "string — .esanim file",
          "defaultClip": "string — animation name to play on attach",
          "speed": "number — playback multiplier, default 1"
        },
        "methods": {
          "play": "animator.play(clip: string, opts?: { loop?: boolean, onComplete?: () => void }): void",
          "pause": "animator.pause(): void",
          "resume": "animator.resume(): void",
          "stop": "animator.stop(): void"
        },
        "properties": {
          "currentClip": "string",
          "currentFrame": "number",
          "isPlaying": "boolean"
        }
      },
      "PhysicsBody": {
        "import": "import { PhysicsBody } from '@emptysock/engine'",
        "props": {
          "shape": "'cuboid' | 'ball' | 'capsule' | 'polygon' | 'compound'",
          "bodyType": "'fixed' | 'dynamic' | 'kinematicPosition' | 'kinematicVelocity'",
          "mass": "number",
          "friction": "number — 0–1",
          "restitution": "number — 0–1 (bounciness)",
          "ccd": "boolean — continuous collision detection, for fast objects",
          "sensor": "boolean — trigger volume, no physical response",
          "collisionGroups": "string[] — named group labels"
        },
        "methods": {
          "applyImpulse": "body.applyImpulse(force: Vector2): void",
          "applyForce": "body.applyForce(force: Vector2): void",
          "setVelocity": "body.setVelocity(vel: Vector2): void",
          "getVelocity": "body.getVelocity(): Vector2"
        }
      },
      "CharacterController": {
        "import": "import { CharacterController } from '@emptysock/engine'",
        "props": {
          "slopeAngle": "number — max walkable slope degrees",
          "snapToGround": "number — snap distance (0 = off)",
          "offset": "number — collision skin width, default 0.01"
        },
        "methods": {
          "moveAndSlide": "controller.moveAndSlide(velocity: Vector2): void",
          "isGrounded": "controller.isGrounded(): boolean",
          "isTouchingCeiling": "controller.isTouchingCeiling(): boolean",
          "isTouchingWall": "controller.isTouchingWall(): 'left' | 'right' | null"
        }
      },
      "PathFollower": {
        "import": "import { PathFollower } from '@emptysock/engine'",
        "props": {
          "path": "Path",
          "speed": "number",
          "onComplete": "() => void — optional"
        },
        "methods": {
          "setPath": "follower.setPath(path: Path): void",
          "stop": "follower.stop(): void",
          "resume": "follower.resume(): void",
          "isComplete": "follower.isComplete(): boolean"
        }
      },
      "RigidJoint": {
        "import": "import { RigidJoint } from '@emptysock/engine'",
        "description": "Component that constrains two PhysicsBody entities with a Rapier joint. Attach to the entity that holds the first PhysicsBody; set bodyBEntityId to the second entity's numeric id.",
        "constructor": "new RigidJoint({ jointType?, bodyBEntityId?, revolute?, prismatic?, spring? })",
        "props": {
          "jointType": "'fixed' | 'revolute' | 'prismatic' | 'spring' — default 'fixed'",
          "bodyBEntityId": "number | null — entity id of the second body"
        },
        "RevoluteOptions": {
          "anchorA": "{ x, y } — anchor on body A in local space",
          "anchorB": "{ x, y } — anchor on body B in local space",
          "minAngle": "number — minimum rotation in radians",
          "maxAngle": "number — maximum rotation in radians"
        },
        "PrismaticOptions": {
          "anchorA": "{ x, y }",
          "anchorB": "{ x, y }",
          "axis": "{ x, y } — slide axis in local space of body A",
          "minDistance": "number",
          "maxDistance": "number"
        },
        "SpringOptions": {
          "anchorA": "{ x, y }",
          "anchorB": "{ x, y }",
          "restLength": "number",
          "stiffness": "number",
          "damping": "number"
        }
      },
      "VNController": {
        "import": "import { VNController } from '@emptysock/engine'",
        "description": "Entity component for simple VN playback. Attach to an entity, set dialogueTree to a .esvn file path, and drive it via advance()/jump(). For programmatic control, callback registration, or VNTextbox binding, use VNSystem instead.",
        "props": {
          "dialogueTree": "string — .esvn file",
          "onChoice": "(index: number, text: string) => void",
          "onEvent": "(name: string, data: unknown) => void"
        },
        "methods": {
          "advance": "vn.advance(): void",
          "jump": "vn.jump(nodeId: string): void",
          "isFinished": "vn.isFinished(): boolean"
        }
      }
    },
    "InputSystem": {
      "import": "import { InputSystem } from '@emptysock/engine'",
      "description": "Instanced input system. Create in onLoad, call attach() to register event listeners. Call flush() once per frame before reading any state.",
      "lifecycle": {
        "attach": "input.attach(target?: EventTarget): void — register listeners (defaults to window)",
        "detach": "input.detach(): void — remove listeners",
        "flush": "input.flush(): void — call once per frame before reading state"
      },
      "keyMethods": {
        "isKeyDown": "input.isKeyDown(code: string): boolean — held every frame",
        "isKeyPressed": "input.isKeyPressed(code: string): boolean — true only on the frame the key went down",
        "isKeyReleased": "input.isKeyReleased(code: string): boolean — true only on the frame the key came up"
      },
      "mouseMethods": {
        "mouseX": "input.mouseX: number",
        "mouseY": "input.mouseY: number",
        "isMouseDown": "input.isMouseDown(button?: number): boolean — default button 0 (left click)",
        "isMousePressed": "input.isMousePressed(button?: number): boolean"
      },
      "touchMethods": {
        "touchCount": "input.touchCount: number",
        "touches": "input.touches: ReadonlyArray<TouchPoint> — {id, x, y, dx, dy}",
        "primaryTouch": "input.primaryTouch: TouchPoint | undefined",
        "isTouchStarted": "input.isTouchStarted(id?: number): boolean",
        "isTouchEnded": "input.isTouchEnded(id?: number): boolean"
      },
      "keyCodes": "KeyboardEvent.code values: 'ArrowLeft' 'ArrowRight' 'ArrowUp' 'ArrowDown' 'Space' 'Enter' 'Escape' 'KeyA'-'KeyZ' 'Digit0'-'Digit9'"
    },
    "Audio": {
      "import": "import { Audio } from '@emptysock/engine'",
      "methods": {
        "play": "Audio.play(id: string, opts?: PlayOptions): AudioHandle",
        "music": "Audio.music(id: string, opts?: MusicOptions): void",
        "stopMusic": "Audio.stopMusic(opts?: { fade?: number }): void",
        "stop": "Audio.stop(id: string): void",
        "setGroupVolume": "Audio.setGroupVolume(group: 'music' | 'sfx' | 'ui' | 'voice', vol: number): void",
        "mute": "Audio.mute(group?: string): void",
        "unmute": "Audio.unmute(group?: string): void"
      },
      "PlayOptions": {
        "volume": "number 0–1",
        "loop": "boolean",
        "spatial": "boolean",
        "position": "{ x: number, y: number }",
        "fade": "number — fade-in seconds",
        "rate": "number — playback speed multiplier"
      }
    },
    "Camera": {
      "import": "import { Camera } from '@emptysock/engine'",
      "methods": {
        "follow": "Camera.follow(entity: Entity, opts?: FollowOptions): void",
        "stopFollow": "Camera.stopFollow(): void",
        "shake": "Camera.shake(opts: { intensity: number, duration: number, decay?: 'linear' | 'exponential' }): void",
        "trauma": "Camera.trauma(amount: number): void",
        "zoom": "Camera.zoom(scale: number, opts?: TweenOptions): void",
        "flash": "Camera.flash(opts: { colour: number, duration: number }): void",
        "fade": "Camera.fade(opts: { to: number, duration: number }): void",
        "unfade": "Camera.unfade(opts?: { duration: number }): void",
        "letterbox": "Camera.letterbox(opts: { ratio: string }): void",
        "pillarbox": "Camera.pillarbox(opts: { ratio: string }): void"
      },
      "FollowOptions": {
        "lerp": "number 0–1",
        "deadzone": "{ x: number, y: number }",
        "lookahead": "{ x: number, y: number }",
        "bounds": "{ x: number, y: number, width: number, height: number }"
      }
    },
    "Timer": {
      "import": "import { Timer } from '@emptysock/engine'",
      "methods": {
        "after": "Timer.after(seconds: number, fn: () => void): TimerHandle",
        "every": "Timer.every(seconds: number, fn: () => void, opts?: { times?: number }): TimerHandle"
      },
      "TimerHandle": {
        "cancel": "handle.cancel(): void"
      }
    },
    "coroutines": {
      "import": "import { waitSeconds, waitFrames, waitUntil, waitForEvent, waitForAnimation, waitForDialogue, waitForPath } from '@emptysock/engine'",
      "usage": "entity.startCoroutine(function* name() { yield helper })",
      "helpers": {
        "waitSeconds": "yield waitSeconds(seconds: number)",
        "waitFrames": "yield waitFrames(count: number)",
        "waitUntil": "yield waitUntil(predicate: () => boolean)",
        "waitForEvent": "yield waitForEvent(eventName: string)",
        "waitForAnimation": "yield waitForAnimation(entity: Entity)",
        "waitForDialogue": "yield waitForDialogue()",
        "waitForPath": "yield waitForPath(entity: Entity)"
      }
    },
    "SaveSystem": {
      "import": "import { SaveSystem } from '@emptysock/engine'",
      "methods": {
        "save": "SaveSystem.save(slot: string, data: unknown): Promise<void>",
        "load": "SaveSystem.load(slot: string): Promise<SaveSlot>",
        "delete": "SaveSystem.delete(slot: string): Promise<void>",
        "listSlots": "SaveSystem.listSlots(): Promise<SaveSlot[]>",
        "enableAutoSave": "SaveSystem.enableAutoSave(opts: { interval: number, slot: string }): void",
        "disableAutoSave": "SaveSystem.disableAutoSave(): void"
      },
      "SaveSlot": {
        "slot": "string",
        "data": "unknown — ALWAYS parse through Zod before use",
        "timestamp": "number",
        "playtime": "number — seconds",
        "scene": "string"
      },
      "settings": {
        "get": "SaveSystem.settings.get<T>(key: string): T | undefined",
        "set": "SaveSystem.settings.set(key: string, value: unknown): Promise<void>"
      }
    },
    "LocalisationSystem": {
      "import": "import { LocalisationSystem } from '@emptysock/engine'",
      "description": "Instanced localisation class. Create one instance; call addTranslations() to register locale data before switching locale.",
      "methods": {
        "setLocale": "instance.setLocale(locale: string): void — switch the active locale",
        "addTranslations": "instance.addTranslations(locale: string, map: Record<string, string>): void — register translations for a locale",
        "t": "instance.t(key: string, vars?: Record<string, string | number>): string — translate key; returns key itself if not found",
        "currentLocale": "instance.currentLocale: string — read-only getter for the active locale"
      },
      "translationFileFormat": {
        "location": "assets/i18n/[locale].json",
        "example": {
          "menu.start": "Start Game",
          "hud.score": "Score: {{score}}",
          "hud.lives": "{{count}} lives"
        }
      }
    },
    "PathfindingSystem": {
      "import": "import { PathfindingSystem } from '@emptysock/engine'",
      "methods": {
        "findPath": "PathfindingSystem.findPath(opts: { from: Point, to: Point, grid: Grid, allowDiagonal?: boolean }): Promise<Path>",
        "buildNavMesh": "PathfindingSystem.buildNavMesh(scene: Scene): NavMesh",
        "debugDraw": "PathfindingSystem.debugDraw(enabled: boolean): void"
      }
    },
    "TilemapSystem": {
      "import": "import { TilemapSystem } from '@emptysock/engine'",
      "methods": {
        "load": "TilemapSystem.load(file: string): TilemapInstance"
      },
      "TilemapInstance": {
        "getLayer": "map.getLayer(name: string): TilemapLayer",
        "width": "number — tiles",
        "height": "number — tiles",
        "tileWidth": "number — pixels",
        "tileHeight": "number — pixels"
      },
      "TilemapLayer": {
        "enablePhysics": "layer.enablePhysics(): void",
        "entities": "Entity[] — object layer placements",
        "getTile": "layer.getTile(x: number, y: number): Tile | null",
        "asGrid": "layer.asGrid(): Grid — for A* pathfinding"
      }
    },
    "ObjectPool": {
      "import": "import { ObjectPool } from '@emptysock/engine'",
      "constructor": "new ObjectPool<T>(EntityClass: new() => T, opts: { size: number })",
      "methods": {
        "acquire": "pool.acquire(): T",
        "release": "pool.release(instance: T): void",
        "size": "pool.size: number",
        "available": "pool.available: number"
      }
    },
    "Profiler": {
      "import": "import { Profiler } from '@emptysock/engine'",
      "methods": {
        "begin": "Profiler.begin(label: string): void",
        "end": "Profiler.end(label: string): void",
        "getStats": "Profiler.getStats(): ProfileStats"
      },
      "ProfileStats": {
        "fps": "number",
        "frameTime": "number — ms",
        "drawCalls": "number",
        "entityCount": "number",
        "physicsTime": "number — ms",
        "renderTime": "number — ms",
        "scriptTime": "number — ms",
        "memoryMB": "number"
      }
    },
    "ParticleEmitter": {
      "import": "import { ParticleEmitter } from '@emptysock/engine'",
      "description": "Component that spawns and animates short-lived visual particles. Attach to an entity, configure options, then call start() or burst().",
      "constructorOptions": {
        "rate": "number — particles emitted per second in continuous mode",
        "lifetime": "number — seconds each particle lives",
        "speed": "number — initial speed in world units/second",
        "spread": "number — emission cone half-angle in degrees (0 = straight line, 360 = sphere)",
        "count": "number (1–2000) — maximum simultaneous live particles",
        "textureName": "string (optional) — asset name for sprite-sheet particles; omit for default white square"
      },
      "methods": {
        "start": "emitter.start(): void — begin continuous emission at the configured rate",
        "stop": "emitter.stop(): void — stop emitting new particles; existing particles finish their lifetime",
        "burst": "emitter.burst(count: number): void — fire exactly count particles immediately, ignoring rate"
      },
      "properties": {
        "rate": "number — read/write, effective before start() is called",
        "lifetime": "number — read/write",
        "speed": "number — read/write",
        "spread": "number — read/write",
        "count": "number — read/write",
        "textureName": "string | undefined — read/write"
      },
      "notes": [
        "Call stop() in onDestroy before the entity is destroyed.",
        "burst() does not require start() to have been called first.",
        "Particles sharing the same textureName are batched into one draw call.",
        "count should be kept as low as visually acceptable — all particles update every frame."
      ]
    },
    "WindowSystem": {
      "description": "Manages window mode, size, title, and position. Wraps Tauri native window API with transparent browser fallback. Import the singleton `windowSystem`.",
      "import": "import { windowSystem, type WindowMode, type WindowConfig } from '@emptysock/engine'",
      "methods": {
        "apply": {
          "signature": "apply(config: Partial<WindowConfig>): Promise<void>",
          "description": "Apply all window settings at once. Call once in root scene onLoad."
        },
        "setMode": {
          "signature": "setMode(mode: WindowMode): Promise<void>",
          "description": "Switch window mode. 'windowed' | 'fullscreen' | 'borderless'."
        },
        "setSize": {
          "signature": "setSize(width: number, height: number): Promise<void>",
          "description": "Resize the window. No-op in fullscreen/borderless modes on desktop."
        },
        "setTitle": {
          "signature": "setTitle(title: string): Promise<void>",
          "description": "Update the window/tab title."
        },
        "setResizable": {
          "signature": "setResizable(resizable: boolean): Promise<void>",
          "description": "Allow or prevent manual resizing. Desktop only."
        },
        "setMinSize": {
          "signature": "setMinSize(width: number, height: number): Promise<void>",
          "description": "Set minimum window dimensions. Desktop only."
        },
        "setPosition": {
          "signature": "setPosition(x: number, y: number): Promise<void>",
          "description": "Move window to screen coordinates. Desktop only."
        },
        "center": {
          "signature": "center(): Promise<void>",
          "description": "Centre window on the current monitor. Desktop only."
        },
        "setAlwaysOnTop": {
          "signature": "setAlwaysOnTop(value: boolean): Promise<void>",
          "description": "Pin window above others. Desktop only."
        }
      },
      "properties": {
        "currentMode": "WindowMode — current window mode",
        "currentConfig": "Readonly<WindowConfig> — full current config snapshot"
      }
    },
    "UISystem": {
      "import": "import { UISystem, LabelWidget, ImageWidget, ButtonWidget, PanelWidget, ProgressBarWidget, SliderWidget, CheckboxWidget } from '@emptysock/engine'",
      "description": "Canvas 2D overlay for screen-space UI (menus, HUD, dialogue boxes). Renders on top of the PixiJS scene. Access via this.uiSystem inside any Scene subclass.",
      "singleton": "UISystem — module-level singleton, shared across all scenes",
      "methods": {
        "add": "UISystem.add(widget: Widget): void — add a root widget to the overlay",
        "removeWidget": "UISystem.removeWidget(widget: Widget): void — remove a root widget",
        "clear": "UISystem.clear(): void — remove all widgets and UIComponents",
        "clearWidgets": "UISystem.clearWidgets(): void — remove only Widget-API roots (not legacy UIComponents)",
        "setImageLoader": "UISystem.setImageLoader(loader: ImageLoader): void — override the default fetch-based loader",
        "update": "UISystem.update(dt: number, pointerX?, pointerY?, cw?, ch?): void — tick animations + hover state",
        "render": "UISystem.render(ctx: IUIRenderer, cw: number, ch: number): void — draw all components and widgets",
        "handleClick": "UISystem.handleClick(x, y, cw, ch): boolean — hit-test and dispatch click",
        "handlePointerMove": "UISystem.handlePointerMove(x, y, cw, ch): void — update hover state"
      },
      "note": "UISystem constructs a default ImageLoader internally (fetch + createImageBitmap). No setup needed for the common case."
    },
    "Widget": {
      "import": "import { Widget, LabelWidget, ImageWidget, ButtonWidget, PanelWidget, ProgressBarWidget, SliderWidget, CheckboxWidget } from '@emptysock/engine'",
      "description": "Base class and 7 concrete widget types for building screen-space UI. Use uiSystem.add(widget) to render, uiSystem.removeWidget(widget) to remove.",
      "baseProperties": {
        "x": "number — pixel offset from anchor",
        "y": "number — pixel offset from anchor",
        "width": "number",
        "height": "number",
        "anchor": "WidgetAnchor — 'top-left'|'top'|'top-right'|'left'|'center'|'right'|'bottom-left'|'bottom'|'bottom-right'",
        "visible": "boolean",
        "alpha": "number (0–1)",
        "children": "Widget[] — mutable; add children with panel.children.push(child)"
      },
      "baseMethods": {
        "on": "widget.on(event: WidgetEvent, handler): void — events: 'click'|'hover'|'hoverOut'|'change'|'animEnd'",
        "off": "widget.off(event, handler): void",
        "animate": "widget.animate(name: AnimationName, opts?): void — names: 'fadeIn'|'fadeOut'|'slideIn'|'slideOut'|'pop'|'shake'; opts: { duration?: number, easing?: string, direction?: 'left'|'right'|'up'|'down' }",
        "triggerClick": "widget.triggerClick(): void",
        "contains": "widget.contains(px, py, cw, ch): boolean"
      },
      "widgets": {
        "LabelWidget": "new LabelWidget({ text?, font?, fontSize?, color?, align?, ...baseOpts })",
        "ImageWidget": "new ImageWidget({ src?, scaleMode?: \"fit\"|\"fill\"|\"stretch\"|\"none\", tint?, ...baseOpts })",
        "ButtonWidget": "new ButtonWidget({ label?, icon?, color?, background?, hoverBackground?, pressedBackground?, borderRadius?, fontSize?, font?, disabled?, animateOnHover?, ...baseOpts }) — state: \"normal\"|\"hover\"|\"pressed\"|\"disabled\"",
        "PanelWidget": "new PanelWidget({ background?, border?, borderWidth?, cornerRadius?, ...baseOpts })",
        "ProgressBarWidget": "new ProgressBarWidget({ value?, min?, max?, fillColor?, trackColor?, direction?: \"h\"|\"v\", ...baseOpts }) — .normalised: number",
        "SliderWidget": "new SliderWidget({ value?, min?, max?, step?, trackColor?, thumbColor?, onChange?, ...baseOpts }) — .normalised: number",
        "CheckboxWidget": "new CheckboxWidget({ checked?, label?, color?, background?, borderColor?, fontSize?, font?, onChange?, ...baseOpts })"
      },
      "example": "const panel = new PanelWidget({ anchor: 'center', width: 300, height: 200 });\nconst btn = new ButtonWidget({ label: 'Resume', anchor: 'center', y: 20 });\nbtn.on('click', () => this.engine.popScene());\npanel.children.push(btn);\nthis.uiSystem.add(panel);\npanel.animate('fadeIn');"
    }
  },
  "fileExtensions": {
    ".esscene": "Scene — entities, components, properties (JSON)",
    ".esmap": "Tilemap — layers, tiles, collision, objects (JSON)",
    ".esanim": "Spritesheet animation clips (JSON)",
    ".esprefab": "Reusable entity template (JSON)",
    ".esvn": "Visual novel dialogue tree (JSON)",
    ".esparticle": "Particle system definition (JSON)",
    ".esui": "UI layout panel (JSON)",
    ".esdata": "Game data file (validated JSON)"
  },
  "postProcessEffects": [
    "bloom",
    "chromatic-aberration",
    "vignette",
    "scanlines",
    "pixelate",
    "colour-grade",
    "blur",
    "outline",
    "shockwave",
    "noise"
  ],
  "gpuTiers": {
    "potato": {
      "examples": "Pi Zero, Pi 1, Mali-G51",
      "maxParticles": 100,
      "maxLights": 1,
      "postFx": false,
      "targetFPS": 30
    },
    "low": {
      "examples": "Pi 4 4GB, S9+ (Mali-G72), 2013 laptop iGPU",
      "maxParticles": 500,
      "maxLights": 4,
      "postFx": "1 pass",
      "targetFPS": 60
    },
    "mid": {
      "examples": "2015 laptop iGPU, GTX 750",
      "maxParticles": 2000,
      "maxLights": 8,
      "postFx": "2 passes",
      "targetFPS": 60
    },
    "high": {
      "examples": "GTX 1060, RX 580, M1",
      "maxParticles": 5000,
      "maxLights": 16,
      "postFx": "full",
      "targetFPS": 60
    },
    "ultra": {
      "examples": "RTX 30+, RX 6000+, M2+",
      "maxParticles": 10000,
      "maxLights": 32,
      "postFx": "full",
      "targetFPS": "unlocked"
    }
  },
  "exportTargets": [
    "web",
    "windows",
    "macos",
    "linux",
    "android",
    "ios",
    "raspi"
  ],
  "commitConvention": {
    "format": "type(scope): description",
    "types": [
      "feat",
      "fix",
      "refactor",
      "perf",
      "chore",
      "docs",
      "test"
    ],
    "examples": [
      "feat(player): add double jump",
      "fix(physics): character clips through thin floor at high speed",
      "refactor(enemy): extract patrol into PatrolComponent",
      "perf(particles): cap emitter on low GPU tier",
      "chore(assets): compress atlas for Pi export"
    ]
  },
  "builtins": {
    "description": "Compile-time constants injected by the EmptySock build pipeline. Replace with literal values at build time — no runtime lookup. Available in all game files without import.",
    "constants": {
      "PROJECT_TITLE": {
        "type": "string",
        "source": "Project window title setting"
      },
      "PROJECT_NAME": {
        "type": "string",
        "source": "Project name"
      },
      "GAME_WIDTH": {
        "type": "number",
        "source": "Project window width setting"
      },
      "GAME_HEIGHT": {
        "type": "number",
        "source": "Project window height setting"
      },
      "DEBUG": {
        "type": "boolean",
        "source": "true in debug build, false in release"
      }
    }
  },
  "AutoTileSystem": {
    "import": "import { AutoTileSystem } from '@emptysock/engine'",
    "description": "Selects tile variants based on 8-neighbour bitmask rules.",
    "methods": {
      "addRuleSet": {
        "signature": "(ruleSet: AutoTileRuleSet): void"
      },
      "removeRuleSet": {
        "signature": "(baseTileIndex: number): void"
      },
      "getRuleSet": {
        "signature": "(baseTileIndex: number): AutoTileRuleSet | undefined"
      },
      "resolve": {
        "signature": "(col: number, row: number, baseTileIndex: number, tileAt: (col: number, row: number) => number): number",
        "description": "Returns the tile index to paint at (col, row) given neighbour occupancy."
      }
    }
  },
  "VariableStore": {
    "import": "import { VariableStore } from '@emptysock/engine'",
    "description": "Indexed integer variables (1-1000) and boolean switches (1-1000) with localStorage persistence.",
    "methods": {
      "getVar": {
        "signature": "(index: number): number"
      },
      "setVar": {
        "signature": "(index: number, value: number): void"
      },
      "getVarName": {
        "signature": "(index: number): string"
      },
      "setVarName": {
        "signature": "(index: number, name: string): void"
      },
      "getSwitch": {
        "signature": "(index: number): boolean"
      },
      "setSwitch": {
        "signature": "(index: number, value: boolean): void"
      },
      "getSwitchName": {
        "signature": "(index: number): string"
      },
      "setSwitchName": {
        "signature": "(index: number, name: string): void"
      },
      "save": {
        "signature": "(): void",
        "description": "Persist to localStorage."
      },
      "load": {
        "signature": "(): void",
        "description": "Restore from localStorage."
      },
      "snapshot": {
        "signature": "(): VariableStoreData"
      },
      "restore": {
        "signature": "(data: VariableStoreData): void"
      },
      "reset": {
        "signature": "(): void",
        "description": "Clear all variables to 0 and all switches to false in memory. Does not persist — call save() afterwards if the reset should survive a reload."
      }
    }
  },
  "MapEventSystem": {
    "import": "import { MapEventSystem } from '@emptysock/engine'",
    "description": "Tile-aligned event system with autorun, player-touch, action-button, and parallel trigger types.",
    "methods": {
      "addEvent": {
        "signature": "(event: MapEvent): void"
      },
      "removeEvent": {
        "signature": "(id: string): void"
      },
      "loadEvents": {
        "signature": "(events: MapEvent[]): void"
      },
      "setHandler": {
        "signature": "(handler: EventCommandHandler): void"
      },
      "update": {
        "signature": "(playerTileX: number, playerTileY: number, actionPressed: boolean): void"
      }
    }
  },
  "GridMovementBehavior": {
    "import": "import { GridMovementBehavior } from '@emptysock/engine'",
    "description": "Smooth 4-directional tile-aligned movement behavior. Attach to an entity via addBehavior().",
    "constructor": "new GridMovementBehavior({ tileSize?, speed?, input, isSolid? })",
    "properties": {
      "tileSize": "number",
      "speed": "number — tiles per second"
    }
  },
  "CharacterStage": {
    "import": "import { CharacterStage } from '@emptysock/engine'",
    "description": "Renders character sprites at left/center/right stage positions with fade transitions.",
    "constructor": "new CharacterStage({ canvasWidth, canvasHeight, baselineY?, maxHeightFraction? })",
    "methods": {
      "show": {
        "signature": "(slot: StageSlot, imagePath: string, opts?: { expression?, fadeDuration? }): void"
      },
      "hide": {
        "signature": "(slot: StageSlot, fadeDuration?: number): void"
      },
      "update": {
        "signature": "(dt: number): void"
      },
      "render": {
        "signature": "(ctx: CanvasRenderingContext2D): void"
      }
    }
  },
  "VNBackgroundLayer": {
    "import": "import { VNBackgroundLayer } from '@emptysock/engine'",
    "description": "Manages a background image and full-screen CG overlay with cross-fade transitions.",
    "constructor": "new VNBackgroundLayer({ canvasWidth, canvasHeight, fadeDuration? })",
    "methods": {
      "setBackground": {
        "signature": "(imagePath: string, opts?: { fadeDuration?, fit? }): void"
      },
      "clearBackground": {
        "signature": "(fadeDuration?: number): void"
      },
      "showCG": {
        "signature": "(imagePath: string, opts?: { fadeDuration?, fit? }): void"
      },
      "clearCG": {
        "signature": "(fadeDuration?: number): void"
      },
      "update": {
        "signature": "(dt: number): void"
      },
      "render": {
        "signature": "(ctx: CanvasRenderingContext2D): void"
      }
    }
  },
  "VNSystem": {
    "import": "import { VNSystem, type DialogueNode, type DialogueTree } from '@emptysock/engine'",
    "description": "Standalone visual novel runtime. Loads a DialogueTree (converted from Story Graph JSON via storyGraphToDialogueTree). Used by VNTextbox as its data source.",
    "constructor": "new VNSystem()",
    "DialogueNode": "type DialogueNode = { type: 'dialogue'; speaker: string; text: string; voice?: string; next?: string; cgPath?: string } | { type: 'choice'; text: string; options: Array<{ label: string; next: string }>; cgPath?: string } | { type: 'event'; eventName: string; data?: Record<string, unknown>; next?: string; cgPath?: string } | { type: 'jump'; target: string; cgPath?: string } | { type: 'variable-set'; variableKey: string; variableValue: unknown; next?: string; cgPath?: string }",
    "DialogueTree": "interface DialogueTree { nodes: Record<string, DialogueNode>; startNode: string }",
    "methods": {
      "load": {
        "signature": "(tree: DialogueTree): void",
        "description": "Load a DialogueTree and begin processing from startNode. Fires onNode immediately for the first node. Not async — use storyGraphToDialogueTree() or JSON.parse() on a fetched file before calling."
      },
      "advance": {
        "signature": "(): void",
        "description": "Advance past a dialogue, event, or variable-set node to its next. Has no effect on choice or jump nodes."
      },
      "selectOption": {
        "signature": "(next: string): void",
        "description": "Navigate to the target node by ID. Use the next field from the chosen option in a choice node."
      },
      "currentNode": {
        "signature": "DialogueNode | null (getter)",
        "description": "The currently active node, or null if not loaded or finished."
      }
    },
    "callbacks": {
      "onNode": "((node: DialogueNode) => void) | null — fired on every node change including after selectOption()",
      "onChoice": "((options: Array<{ label: string; next: string }>) => void) | null — fired for choice nodes",
      "onEvent": "((eventName: string, data: Record<string, unknown>) => void) | null — fired for event nodes",
      "onEnd": "(() => void) | null — fired when a dialogue node has no next",
      "onCGNode": "((cgPath: string) => void) | null — fired for any node with a cgPath field"
    },
    "notes": [
      "Set callbacks before calling load(). onNode fires immediately on load().",
      "jump nodes are resolved automatically — onNode is never called for them.",
      "variable-set nodes are also resolved automatically (auto-advanced); read variableKey/variableValue inside onNode before calling advance().",
      "No destroy() method — GC'd with the scene when the reference drops.",
      "For save/resume: store the currentNode?.next or the chosen option's next field; call load() again with the full tree and navigate with selectOption() to restore position."
    ]
  },
  "VNTextbox": {
    "import": "import { VNTextbox, type VNTextboxOptions } from '@emptysock/engine'",
    "description": "Pre-built dialogue box rendered by UISystem. Bind to a VNSystem instance to auto-update. Clicking the textbox automatically calls VNSystem.advance(). Call destroy() in onDestroy.",
    "constructor": "new VNTextbox({ canvasWidth, canvasHeight, height?, namePlateHeight?, paddingX?, panelColor?, namePlateColor?, textColor?, fontSize? })",
    "methods": {
      "bind": {
        "signature": "(vn: VNSystem): void",
        "description": "Wire to a VNSystem; immediately syncs to the current node."
      },
      "visible": {
        "signature": "boolean (getter/setter)"
      },
      "destroy": {
        "signature": "(): void",
        "description": "Remove textbox from UISystem. Call in onDestroy."
      }
    }
  },
  "VNScriptConvert": {
    "import": "import { storyGraphToDialogueTree, dialogueTreeToStoryGraph } from '@emptysock/engine'",
    "description": "Converts between Story Graph (.storyGraph.json, visual-editor format) and the DialogueTree format consumed by VNSystem.load().",
    "functions": {
      "storyGraphToDialogueTree": {
        "signature": "(graph: StoryGraph): DialogueTree"
      },
      "dialogueTreeToStoryGraph": {
        "signature": "(tree: DialogueTree): StoryGraph"
      }
    }
  },
  "CGGallery": {
    "import": "import { CGGallery } from '@emptysock/engine'",
    "description": "Tracks CG image unlock state, persisted via SaveSystem.",
    "constructor": "new CGGallery({ entries: CGEntry[], saveSystem?, saveSlot? })",
    "methods": {
      "load": {
        "signature": "(): void"
      },
      "unlock": {
        "signature": "(id: string): void"
      },
      "unlockFromNode": {
        "signature": "(cgId: string): void"
      },
      "isUnlocked": {
        "signature": "(id: string): boolean"
      }
    },
    "properties": {
      "entries": "ReadonlyArray<CGEntry>",
      "unlockedEntries": "CGEntry[]",
      "totalCount": "number",
      "unlockedCount": "number"
    }
  },
  "LightingSystem": {
    "import": "import { LightingSystem } from '@emptysock/engine'",
    "description": "2D normal-map lighting pass. Manages a registry of lights and blends them over the scene. Requires `lighting: true` in SceneConfig.",
    "constructor": "new LightingSystem()",
    "methods": {
      "addLight": {
        "signature": "(config: Light): void",
        "description": "Register a light. Use a unique `id` string so you can remove it or mutate it via lights.get(id) later."
      },
      "removeLight": {
        "signature": "(id: string): boolean",
        "description": "Unregister a light by id. Returns true if it was found."
      },
      "setAmbient": {
        "signature": "(colour: number, intensity: number): void",
        "description": "Set the scene-wide ambient light colour (hex) and intensity (0–1)."
      },
      "attachFilter": {
        "signature": "(stage: Container, useNormalMap?: boolean, canvasWidth?: number, canvasHeight?: number): void",
        "description": "Attach the GPU lighting shader to the scene stage. Call once in onLoad. Without this, lights are registered but not rendered."
      },
      "detachFilter": {
        "signature": "(): void",
        "description": "Remove the GPU filter from the stage. Call in onDestroy."
      },
      "update": {
        "signature": "(dt: number): void",
        "description": "Upload current light positions and colours to GPU uniforms. Call once per frame in onUpdate."
      }
    },
    "properties": {
      "lights": "Map<string, Light> — mutable registry; update light.x/light.y in-place to move lights without remove/re-add",
      "ambientColour": "number — current ambient colour (hex)",
      "ambientIntensity": "number — current ambient intensity (0–1)"
    },
    "Light": {
      "id": "string — unique identifier (required)",
      "type": "LightType — 'point' | 'directional' | 'spot' | 'ambient'",
      "colour": "number — hex colour (mutable)",
      "intensity": "number — brightness multiplier (mutable)",
      "radius": "number — falloff radius in world pixels (point/spot only)",
      "angle": "number — cone half-angle in radians (spot only)",
      "direction": "{ x: number, y: number } — world-space direction (directional only)",
      "castShadows": "boolean",
      "x": "number — world-space x (point/spot; mutable — update each frame to follow entities)",
      "y": "number — world-space y (point/spot; mutable)"
    },
    "notes": [
      "Requires scene config `lighting: true`.",
      "Mutate light.x and light.y directly for moving lights — no need to remove/re-add each frame.",
      "Normal maps: if hero_n.png exists alongside hero.png, pass useNormalMap: true to attachFilter.",
      "Max 16 point lights and 4 directional lights per GPU uniform batch."
    ]
  },
  "NavMeshSystem": {
    "import": "import { NavMeshSystem, type NavMeshData, type NavPolygon, type Vec2 } from '@emptysock/engine'",
    "description": "A* polygon navmesh for entity pathfinding. Accepts pre-built navmesh data (offline). Does not generate navmesh from tilemaps at runtime.",
    "constructor": "new NavMeshSystem()",
    "methods": {
      "load": {
        "signature": "(data: NavMeshData): void",
        "description": "Load a pre-built polygon graph. Build this offline in the level editor and ship as a JSON asset."
      },
      "findPath": {
        "signature": "(from: Vec2, to: Vec2): Vec2[]",
        "description": "Find a path between two world-space points. Returns an array of waypoints; returns [] if no path exists. Synchronous."
      },
      "update": {
        "signature": "(dt: number): void",
        "description": "Reserved for dynamic obstacle updates. Call once per frame in onUpdate."
      }
    },
    "NavMeshData": {
      "polygons": "ReadonlyArray<NavPolygon>"
    },
    "NavPolygon": {
      "id": "number",
      "vertices": "ReadonlyArray<Vec2> — convex polygon vertices in order",
      "centroid": "Vec2",
      "neighbours": "ReadonlyArray<number> — ids of adjacent walkable polygons"
    },
    "Vec2": {
      "x": "number",
      "y": "number"
    },
    "notes": [
      "NavMesh data is offline — no runtime generation from tilemap data.",
      "Use PathfindingSystem for grid-based A* on a TilemapLayer.asGrid().",
      "NavMeshSystem uses polygon-graph A* and is suited for open-world navigation."
    ]
  },
  "CoroutineSystem": {
    "import": "import { CoroutineSystem, waitFrames, waitSeconds, waitUntil, type CoroutineGen } from '@emptysock/engine'",
    "description": "Lightweight coroutine runner. Drives generator functions over time. Entity.startCoroutine() uses this internally; use CoroutineSystem directly when you need named coroutines outside of an entity context.",
    "constructor": "new CoroutineSystem()",
    "methods": {
      "start": {
        "signature": "(id: string, gen: CoroutineGen): void",
        "description": "Start a named coroutine. If a coroutine with the same id is already running it is replaced."
      },
      "stop": {
        "signature": "(id: string): void",
        "description": "Stop a running coroutine by name."
      },
      "update": {
        "signature": "(deltaTime: number): void",
        "description": "Advance all running coroutines. Call once per frame."
      }
    },
    "helpers": {
      "waitFrames": "yield waitFrames(n: number) — pause for n frames",
      "waitSeconds": "yield waitSeconds(t: number) — pause for t seconds",
      "waitUntil": "yield waitUntil(fn: () => boolean) — pause until predicate returns true"
    }
  },
  "GamepadSystem": {
    "import": "import { GamepadSystem, type GamepadState, type DualRumbleOptions } from '@emptysock/engine'",
    "description": "Low-level gamepad polling. Wraps the browser Gamepad API. For most input needs, use Input.axis() and Input.isPressed() instead — they read from GamepadSystem internally.",
    "constructor": "new GamepadSystem()",
    "methods": {
      "update": {
        "signature": "(): void",
        "description": "Poll all connected gamepads. Call once per frame before reading state."
      },
      "getState": {
        "signature": "(index: number): GamepadState | null",
        "description": "Get the current state of gamepad at index (0-based). Returns null if not connected."
      },
      "rumble": {
        "signature": "(index: number, intensity: number, duration: number): void",
        "description": "Simple equal-magnitude rumble on both motors."
      },
      "rumbleDual": {
        "signature": "(index: number, opts: DualRumbleOptions): void",
        "description": "Independent weak and strong motor rumble."
      }
    },
    "GamepadState": {
      "connected": "boolean",
      "buttons": "ReadonlyArray<boolean> — button pressed states",
      "axes": "ReadonlyArray<number> — axis values (-1 to 1)"
    },
    "DualRumbleOptions": {
      "weakMagnitude": "number — 0–1",
      "strongMagnitude": "number — 0–1",
      "duration": "number — milliseconds"
    }
  },
  "TweenManager": {
    "import": "import { TweenManager, type TweenOptions, type EasingName } from '@emptysock/engine'",
    "description": "Interpolates numeric properties on objects over time. Also provides scene-local timers. Use one TweenManager per scene; call update(dt) each frame; destroy in onDestroy.",
    "constructor": "new TweenManager()",
    "methods": {
      "to": {
        "signature": "(target: Record<string, number>, props: Record<string, number>, options: TweenOptions): void",
        "description": "Tween numeric properties of target from their current values to the values in props."
      },
      "after": {
        "signature": "(seconds: number, fn: () => void): void",
        "description": "Run fn once after seconds. Scene-local timer managed by this TweenManager."
      },
      "every": {
        "signature": "(seconds: number, fn: () => void): void",
        "description": "Run fn repeatedly every seconds. Continues until the TweenManager is destroyed."
      },
      "update": {
        "signature": "(deltaTime: number): void",
        "description": "Advance all active tweens and timers. Call once per frame in onUpdate(dt)."
      }
    },
    "TweenOptions": {
      "duration": "number — seconds",
      "ease": "EasingName — default 'linear'",
      "delay": "number — seconds before tween starts",
      "onComplete": "() => void — optional callback"
    },
    "easings": ["linear", "sineIn", "sineOut", "sineInOut", "quadIn", "quadOut", "quadInOut", "cubicIn", "cubicOut", "cubicInOut", "bounceOut", "elasticOut"]
  },
  "PostProcessSystem": {
    "import": "import { PostProcessSystem, type PostEffectType, type PostEffectOptions, type LayerFilterOptions } from '@emptysock/engine'",
    "description": "Instance-based post-processing pipeline. Create one per scene; call update(dt) each frame for transient effects to decay. Effects are composited in add order.",
    "constructor": "new PostProcessSystem()",
    "methods": {
      "add": {
        "signature": "(type: PostEffectType, options?: PostEffectOptions): this",
        "description": "Add or replace a full-screen effect. Returns this for method chaining."
      },
      "remove": {
        "signature": "(type: PostEffectType): void",
        "description": "Remove a full-screen effect by type."
      },
      "has": {
        "signature": "(type: PostEffectType): boolean",
        "description": "Whether the effect is currently active."
      },
      "get": {
        "signature": "(type: PostEffectType): ActiveEffect | undefined",
        "description": "Read the current options for an active effect."
      },
      "flash": {
        "signature": "(opts?: { colour?: number, duration?: number }): void",
        "description": "Transient full-screen flash. colour is a hex number (default 0xffffff); duration in seconds (default 0.15)."
      },
      "setLayerFilter": {
        "signature": "(layerId: string, filter: LayerFilterOptions): void",
        "description": "Apply a CSS filter to a named render layer."
      },
      "clearLayerFilter": {
        "signature": "(layerId: string): void",
        "description": "Remove the filter from a layer."
      },
      "toggleLayerFilter": {
        "signature": "(layerId: string, enabled: boolean): void",
        "description": "Enable or disable a layer's filter without removing it."
      },
      "getLayerFilter": {
        "signature": "(layerId: string): LayerFilterOptions | undefined",
        "description": "Read the current layer filter."
      },
      "update": {
        "signature": "(dt: number): void",
        "description": "Decay transient effects (flash, shockwave). Call once per frame in onUpdate."
      },
      "clear": {
        "signature": "(): void",
        "description": "Remove all effects and reset scene transitions."
      },
      "destroy": {
        "signature": "(): void",
        "description": "Clear all effects and layer filters. Call in onDestroy."
      }
    },
    "PostEffectType": "'bloom' | 'chromatic-aberration' | 'vignette' | 'scanlines' | 'pixelate' | 'colour-grade' | 'blur' | 'outline' | 'shockwave' | 'noise'",
    "LayerFilterType": "'blur' | 'colour-grade' | 'outline' | 'brightness' | 'contrast' | 'saturate' | 'hue-rotate' | 'invert' | 'none'"
  },
  "LayerSystem": {
    "import": "import { LayerSystem, LAYER, type LayerConfig, type LayerSortKey } from '@emptysock/engine'",
    "description": "Instance-based named render-layer manager. Entities are assigned to a layer at an explicit depth; RenderSystem draws layers in ascending index order, then entities within a layer by ascending depth. Four built-in layers are pre-created by the constructor.",
    "constructor": "new LayerSystem()",
    "LAYER": {
      "BACKGROUND": -1000,
      "DEFAULT": 0,
      "FOREGROUND": 100,
      "UI": 1000
    },
    "builtinLayers": ["background", "default", "foreground", "ui"],
    "methods": {
      "defineLayer": {
        "signature": "(name: string, index: number): void",
        "description": "Define or redefine a layer. Lower index = drawn behind. Use index gaps (0, 50, 80, 100) so layers can be inserted later without renumbering."
      },
      "addEntity": {
        "signature": "(entityId: number, layerName: string, depth?: number): void",
        "description": "Assign an entity to a layer at an optional depth (default 0). Unknown layerName falls back to 'default' with a warning."
      },
      "removeEntity": {
        "signature": "(entityId: number): void",
        "description": "Unregister an entity from the layer system. Entity is not destroyed."
      },
      "setDepth": {
        "signature": "(entityId: number, depth: number): void",
        "description": "Update an entity's depth within its current layer without changing the layer."
      },
      "getEntityLayer": {
        "signature": "(entityId: number): string | null",
        "description": "Returns the entity's current layer name, or null if unregistered."
      },
      "getEntityDepth": {
        "signature": "(entityId: number): number",
        "description": "Returns the entity's current depth, or 0 if unregistered."
      },
      "getSortKey": {
        "signature": "(entityId: number): LayerSortKey",
        "description": "Returns [layerIndex, depth] for use in a sort comparator. Unregistered entities get [LAYER.DEFAULT, 0]."
      },
      "getLayerIndex": {
        "signature": "(name: string): number",
        "description": "Returns the index of a named layer, or LAYER.DEFAULT if unknown."
      },
      "setVisible": {
        "signature": "(name: string, visible: boolean): void",
        "description": "Show or hide an entire layer."
      },
      "isVisible": {
        "signature": "(name: string): boolean",
        "description": "Returns true if the layer is visible."
      },
      "getEntitiesOnLayer": {
        "signature": "(layerName: string): Array<{ entityId: number; depth: number }>",
        "description": "All entities on a layer, sorted by depth ascending."
      },
      "getLayersSorted": {
        "signature": "(): LayerConfig[]",
        "description": "All layer configs sorted by index ascending (render order). LayerConfig: { name, index, visible }."
      },
      "destroy": {
        "signature": "(): void",
        "description": "Clear all placements and layers. Call in onDestroy."
      }
    },
    "notes": [
      "Use LAYER constants for the four built-in layer indices.",
      "depth within a layer controls fine-grained draw order within the same layer.",
      "Unregistered entities fallback to [LAYER.DEFAULT, 0] in getSortKey."
    ]
  },
  "BattleSystem": {
    "import": "import { BattleSystem, type BattlePhase, type BattleStats, type StatusEffect, type Combatant, type SkillTargetType, type DamageFormulaId, type SkillDef, type StatusEffectDef, type BattleDatabase, type BattleAction, type BattleEvent, type BattleSystemOptions } from '@emptysock/engine'",
    "description": "Opt-in turn-based RPG battle module. Handles turn order (speed desc), damage resolution, status effects, and event emission. Tree-shaken from bundles that do not import it. Enemies act automatically (attack lowest-HP party member); party members require submitAction().",
    "constructor": "new BattleSystem(options?: BattleSystemOptions)",
    "BattleSystemOptions": {
      "db": "BattleDatabase — optional, can also be loaded via loadDatabase()",
      "critChance": "number — default 0.0625",
      "critMultiplier": "number — default 1.5",
      "fleeChance": "number — default 0.5"
    },
    "methods": {
      "addPartyMember": {
        "signature": "(combatant: Combatant): void"
      },
      "addEnemy": {
        "signature": "(combatant: Combatant): void"
      },
      "loadDatabase": {
        "signature": "(db: BattleDatabase): void"
      },
      "setDamageFormula": {
        "signature": "(fn: (atk: number, def: number, power: number, isCrit: boolean, critMultiplier: number) => number): void"
      },
      "onEvent": {
        "signature": "(handler: (event: BattleEvent) => void): () => void",
        "description": "Subscribe to battle events. Returns an unsubscribe function."
      },
      "start": {
        "signature": "(): void",
        "description": "Begin the battle. Fires battle-start, round-start, then action-needed for the first party member."
      },
      "submitAction": {
        "signature": "(combatantId: string, action: BattleAction): void",
        "description": "Submit a party member's action. Only valid when getPhase() === 'input'."
      },
      "getPhase": {
        "signature": "(): BattlePhase"
      },
      "getCombatant": {
        "signature": "(id: string): Combatant | undefined"
      },
      "getParty": {
        "signature": "(): readonly Combatant[]"
      },
      "getEnemies": {
        "signature": "(): readonly Combatant[]"
      },
      "getRound": {
        "signature": "(): number"
      },
      "destroy": {
        "signature": "(): void",
        "description": "Required in onDestroy()."
      }
    },
    "types": {
      "BattlePhase": "'idle' | 'input' | 'resolving' | 'victory' | 'defeat'",
      "BattleStats": "{ hp, maxHp, mp, maxMp, attack, defense, speed, luck: number }",
      "StatusEffect": "{ id: string; name: string; turnsRemaining: number } — turnsRemaining -1 = permanent",
      "Combatant": "{ id: string; name: string; stats: BattleStats; statusEffects: readonly StatusEffect[]; isParty: boolean }",
      "SkillTargetType": "'single-enemy' | 'all-enemies' | 'single-ally' | 'all-allies' | 'self'",
      "DamageFormulaId": "'physical' | 'magical' | 'fixed' | 'percent-max-hp'",
      "SkillDef": "{ id, name, mpCost, targetType: SkillTargetType, formula: DamageFormulaId, power: number, isHeal?: boolean, statusEffect?: { effectId: string; chance: number } }",
      "StatusEffectDef": "{ id, name, hpDrainPercentPerTurn?: number, attackMultiplier?: number, defenseMultiplier?: number }",
      "BattleDatabase": "{ skills: SkillDef[]; statusEffects: StatusEffectDef[] }",
      "BattleAction": "{ type: 'attack'; targetId: string } | { type: 'skill'; skillId: string; targetId: string } | { type: 'flee' }",
      "BattleEvent": "{ kind: 'battle-start' } | { kind: 'round-start'; round: number } | { kind: 'action-needed'; combatantId: string } | { kind: 'damage'; sourceId: string; targetId: string; amount: number; isCrit: boolean } | { kind: 'heal'; sourceId: string; targetId: string; amount: number } | { kind: 'mp-cost'; combatantId: string; amount: number } | { kind: 'status-applied'; combatantId: string; effectId: string; name: string } | { kind: 'status-expired'; combatantId: string; effectId: string } | { kind: 'combatant-defeated'; combatantId: string } | { kind: 'victory' } | { kind: 'defeat' } | { kind: 'fled' }"
    }
  }
}